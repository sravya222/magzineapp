name: Deploy AWS Resources

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: ap-south-2  # Set your AWS region

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Configure AWS credentials
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      # 3️⃣ Update existing Lambda functions (flattened)
      - name: Update existing Lambda functions
        run: |
          echo "📦 Updating Lambda functions..."
          mkdir -p temp_lambda

          for file in lambda_functions/*.zip; do
            LAMBDA_NAME=$(basename "$file" .zip)
            echo "🔹 Processing $LAMBDA_NAME"

            # Extract ZIP
            rm -rf temp_lambda/*
            unzip -q "$file" -d temp_lambda

            # Flatten folder structure (index.js at root)
            INNER_DIR=$(find temp_lambda -mindepth 1 -maxdepth 1 -type d | head -n 1 || true)
            if [ -n "$INNER_DIR" ]; then
              mv "$INNER_DIR"/* temp_lambda/
              rm -rf "$INNER_DIR"
            fi

            # Re-zip flat structure
            cd temp_lambda
            zip -r "../$LAMBDA_NAME.zip" . >/dev/null
            cd ..

            # Update Lambda function (skip creation)
            if aws lambda get-function --function-name "$LAMBDA_NAME" --region $AWS_REGION > /dev/null 2>&1; then
              echo "🔄 Updating Lambda: $LAMBDA_NAME"
              aws lambda update-function-code \
                --function-name "$LAMBDA_NAME" \
                --zip-file "fileb://$LAMBDA_NAME.zip" \
                --region $AWS_REGION
            else
              echo "⚠️ Lambda $LAMBDA_NAME does not exist — skipping creation"
            fi

            rm -f "$LAMBDA_NAME.zip"
          done

          rm -rf temp_lambda
          echo "✅ Lambda update complete"

      # 4️⃣ Deploy API Gateway (safe update)
      - name: Deploy API Gateway
        run: |
          if [ -f "apis.json" ]; then
            echo "🌐 Deploying API from apis.json"

            EXISTING_API_ID=$(aws apigatewayv2 get-apis \
              --region $AWS_REGION \
              --query "Items[?Name=='tsrmagazine'].ApiId" \
              --output text)

            if [ -n "$EXISTING_API_ID" ] && [ "$EXISTING_API_ID" != "None" ]; then
              echo "🔄 Updating existing API (ID: $EXISTING_API_ID)"
              aws apigatewayv2 update-api \
                --cli-input-json file://apis.json \
                --api-id "$EXISTING_API_ID" \
                --region $AWS_REGION
            else
              echo "🆕 No existing API found — importing new API"
              aws apigatewayv2 import-api \
                --cli-input-json file://apis.json \
                --region $AWS_REGION
            fi
          else
            echo "⚠️ apis.json not found — skipping API deployment"
          fi
